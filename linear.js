// Big O(n) Function

function linearFunc(arr) {
    for (let i = 0; i < arr.length; i++) {
        console.log(1000 * 10000)
    }
}

const arr = [1, 2, 3, 4, 5, 6, 7]
linearFunc(arr)

// an example of how say how long a dentist would take to treat x amount of patients